# Contextual selectors

Demonstrates contextual (combinator) selectors that target elements based on their position relative to others. Open `index.html` in any browser to view it.

Note: `Flags.png` is a placeholder image, since the original was not included with the source material.
