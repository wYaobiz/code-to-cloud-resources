# Font families and sizes

Demonstrates setting different `font-family` values on the body, headings, and paragraphs, along with a page background color. Open `index.html` in any browser to view it.

Note: parts of this example (such as the surrounding HTML structure) were reconstructed from the original course material, which was incomplete in places.
