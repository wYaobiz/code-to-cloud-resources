# Box model without box-sizing

Demonstrates the default box model with no `box-sizing` property, so padding adds to the element's declared width and height, making the padded box larger. Open `index.html` in any browser to view it.
