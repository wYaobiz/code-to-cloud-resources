# Font types and the font shorthand

Demonstrates several font families and sizes (px, em, percent, pt), ending with the `font` shorthand property that combines style, variant, weight, size, line height, and family in one declaration. Open `index.html` in any browser to view it.

Note: parts of this example (such as the surrounding HTML structure) were reconstructed from the original course material, which was incomplete in places.
