# Pseudo-class selectors

Demonstrates pseudo-class selectors such as :hover and :nth-child that target elements in a particular state or position. Open `index.html` in any browser to view it.

Note: `Flags.png` is a placeholder image, since the original was not included with the source material.
