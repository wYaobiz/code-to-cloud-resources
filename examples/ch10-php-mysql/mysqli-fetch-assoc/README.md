# mysqli_fetch_assoc()

Reads each row as an associative array, so columns are accessed by name.

This is a server-side example. It assumes a `connect.php` that holds the database connection details, and it queries a sample `Pets` table. The code is provided for reference; it runs on a server with PHP and MySQL rather than in the browser.
