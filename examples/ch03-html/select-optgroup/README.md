# Grouped options with optgroup

Demonstrates the `optgroup` element, which groups related options under a labeled heading inside a `select` dropdown. Open `index.html` in any browser to view it.

Note: the source example here corrects a few items from the original draft, where some cities were grouped under the wrong country, two names were misspelled, and one `optgroup` was missing its closing tag.
