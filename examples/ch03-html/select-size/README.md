# Select with size attribute

Demonstrates the `size` attribute on a `select` element, which displays a fixed number of options as a scrollable list rather than a single-line dropdown. Open `index.html` in any browser to view it.
