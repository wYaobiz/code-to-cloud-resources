# Table with rowspan

Demonstrates the `rowspan` attribute, which lets a single cell span two rows, here used so each "Title of Book" header covers two book titles by the same author. Open `index.html` in any browser to view it.
