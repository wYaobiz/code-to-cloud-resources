# Overflow property

Demonstrates the `overflow` property with the value `visible`, where content that does not fit its box spills outside rather than being clipped. Open `index.html` in any browser to view it.

Note: parts of this example (such as the surrounding HTML structure) were reconstructed from the original course material, which was incomplete in places.
