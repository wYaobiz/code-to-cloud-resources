# Box model with box-sizing

Demonstrates the box model with `box-sizing: border-box`, so padding is drawn inside the declared width and height rather than adding to them. Open `index.html` in any browser to view it.
