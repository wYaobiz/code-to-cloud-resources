# Inheritance of properties

Demonstrates property inheritance, where child elements inherit values such as `font-size` and `font-family` from a parent, while each element can still set its own color. Open `index.html` in any browser to view it.

Note: parts of this example (such as the surrounding HTML structure) were reconstructed from the original course material, which was incomplete in places.
