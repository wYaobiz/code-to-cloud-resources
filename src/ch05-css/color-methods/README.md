# Methods for setting color

Demonstrates several ways to set color in CSS: `rgba`, `hsl`, and `hsla`, including the use of an alpha channel for opacity. Open `index.html` in any browser to view it.

Note: parts of this example (such as the surrounding HTML structure) were reconstructed from the original course material, which was incomplete in places.
