# Border property

Demonstrates the `border` property: border styles (dotted, solid, dashed), widths, and setting colors per side using `border-top-color` and the four-value `border-color` shorthand. Open `index.html` in any browser to view it.

Note: parts of this example (such as the surrounding HTML structure) were reconstructed from the original course material, which was incomplete in places.
