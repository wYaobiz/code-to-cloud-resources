# Escape characters

Uses the backslash escape character to include special characters in strings.

This is a server-side PHP example, provided as source code. It runs on a server with PHP rather than in the browser.
