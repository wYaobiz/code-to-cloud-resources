# Multiple selection dropdown

Demonstrates a `select` element with the `multiple` attribute, allowing several options to be chosen at once, and the `selected` attribute to preselect one. Open `index.html` in any browser to view it.
