# Login form

Combines text, password, select, and submit controls into one form that posts to a PHP script. Open `index.html` in any browser to view the layout. Note that the form's action points to a PHP script, so actually submitting it requires a server with that script in place.
