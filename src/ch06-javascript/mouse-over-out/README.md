# Mouse over and out

Responds to the mouseover and mouseout events by changing the styling of a text element. Open `index.html` in any browser to view it.

Note: the inline script was reconstructed from the course material, which referenced an external JavaScript file that was not included.
